{
  "name": "olympus-pro",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ drondin/olympus-pro",
    "create-local": "graph create --node http://localhost:8020/ drondin/olympus-pro",
    "remove-local": "graph remove --node http://localhost:8020/ drondin/olympus-pro",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 drondin/olympus-pro"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.21.0",
    "@graphprotocol/graph-ts": "0.20.0"
  }
}
